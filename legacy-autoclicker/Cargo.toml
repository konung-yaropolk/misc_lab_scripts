[package]
name = "legacy-autoclicker"
version = "0.1.0"
edition = "2021"

[dependencies]
enigo = { version = "0.6", features = ["serde"] }  # serde optional but useful
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"